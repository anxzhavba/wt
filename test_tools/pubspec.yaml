name: test_tools
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  mqtt_client: ^9.6.4
  http: ^0.13.6
  flutter_blue_plus: ^1.4.0
  shared_preferences: ^2.1.0
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.12.0

flutter_icons:
  android: true
  ios: true
  image_path: lib/icon.jpg

flutter:
  uses-material-design: true
